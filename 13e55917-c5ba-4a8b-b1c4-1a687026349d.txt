Du sollst mein bestehendes Roblox-Spiel erweitern und mehrere bestehende Systeme korrigieren. Arbeite auf Basis meines vorhandenen Codes und meiner vorhandenen Struktur. Entferne oder zerstöre keine bestehenden Funktionen, die nicht ausdrücklich geändert werden sollen.

WICHTIGE ALLGEMEINE REGELN

1. Bestehende Systeme nicht unnötig komplett neu schreiben. Nutze die vorhandene Architektur, Remotes, DataStore-Struktur, UI-Hierarchie, Events und vorhandenen Performance-Systeme, soweit möglich.

2. Keine Dummy-Lösungen, keine vereinfachten Beispiele und kein Pseudocode. Ich brauche vollständig funktionsfähigen Roblox-Lua-Code.

3. Wenn mehrere Scripts betroffen sind, gib mir die vollständigen, fertig ersetzbaren Scripts und nicht nur einzelne Code-Fragmente oder Patches.

4. Wenn ein altes System durch eine neue Logik ersetzt wird, darf das alte System nicht parallel weiterlaufen. Entferne doppelte Loops, doppelte RemoteEvents, doppelte Handler und alte Wrapper, sofern sie durch die neue Lösung nicht mehr benötigt werden.

5. Bestehende Namen und Hierarchien sollen weiterverwendet werden, wenn ich sie ausdrücklich genannt habe.

6. Die Lösung muss serverseitig sicher sein. Belohnungen, Gamepass-Prüfungen, Daily-Reward-Claims, Ränge, Berechtigungen und Änderungen an Spielerdaten dürfen nicht nur clientseitig kontrolliert werden.

7. Performance ist sehr wichtig. Vermeide unnötige Heartbeat-Loops, RenderStepped-Loops, redundante Berechnungen, unnötige RemoteEvents und wiederholtes Setzen derselben Properties.

8. Nutze für die Performance des Event-Systems das bereits vorhandene neue Chunk-System aus meinem ChunkLoader. Erstelle dafür kein zusätzliches ineffizientes System.

9. Das vorhandene responsive UI- und UIScale-System darf durch die neuen Systeme nicht kaputt gemacht werden.

10. Das bestehende PC-Layout bleibt die Referenz. Mobile, Tablet, Console, VR und andere Auflösungen sollen responsiv angepasst werden und nicht einfach nur wie ein verkleinertes PC-UI aussehen.

11. Keine Änderungen an Fonts, Schriften oder bestehenden Designs, außer wenn ich es ausdrücklich verlange.


ALTER X2-SPEED-GAMEPASS WIRD ZUM X2-WIN-PASS

Der bisherige x2 Speed Gamepass soll vollständig zu einem x2 Win Pass geändert werden.

Die bisherige Funktion des Passes, Speed zu verdoppeln, darf nicht mehr verwendet werden.

Stattdessen soll der Pass folgendes machen:

Wer diesen Gamepass besitzt, darf die VIP-Plattformen benutzen.

Die VIP-Plattformen müssen serverseitig prüfen, ob der Spieler diesen Gamepass besitzt.

Wenn der Spieler den Pass bereits besitzt und trotzdem auf den Kauf-Button klickt, darf kein weiterer Kauf und keine weitere Belohnung ausgelöst werden.

Im Feedback soll dann in roter Farbe exakt dieser Text erscheinen:

You already bought this item

Der bestehende Feedback-Stil soll ansonsten erhalten bleiben.

Wichtig:

Der Gamepass darf nicht mehr als Speed-Multiplikator verwendet werden.

Der neue Zweck des Passes ist ausschließlich der Zugriff auf die VIP-Plattformen.

Alle alten Variablen, Namen und Logiken, die noch ausschließlich für den alten x2 Speed Pass verwendet wurden, sollen sauber angepasst werden, damit keine alte x2-Speed-Logik im Hintergrund weiterläuft.


NEUES DAILY-REWARD-SYSTEM

Es gibt den Button:

dailyrewards_openbutton

Wenn dieser Button angeklickt wird, soll innerhalb von popups der Frame main von dailyrewards geöffnet werden.

Die Struktur enthält:

day1
day2
day3
day4
day5
day6
day7
day8

Die Rewards sind exakt:

day1 = +5 Speed
day2 = +10 Wins
day3 = +4 Rebirths
day4 = +50 Speed
day5 = +20 Wins
day6 = +25 Wins
day7 = +80 Speed
day8 = +25 Rebirths

Der Spieler soll beim allerersten Join direkt day1 claimen können.

Nachdem day1 geclaimed wurde, wird am nächsten Tag day2 freigeschaltet.

Danach:

nächster Tag = day3
nächster Tag = day4
nächster Tag = day5
nächster Tag = day6
nächster Tag = day7
nächster Tag = day8

Das System darf nicht zurückgesetzt werden, wenn der Spieler mehrere Tage nicht online war.

Wenn ein Spieler beispielsweise day1 claimt und danach zwei oder mehr Tage nicht online kommt, muss der Fortschritt anhand der vergangenen Zeit weiterlaufen.

Verpasste Tage dürfen nicht verloren gehen.

Bereits freigeschaltete, aber noch nicht geclaimte Rewards müssen erhalten bleiben.

Wenn mehrere Tage während der Offline-Zeit vergangen sind, können mehrere Rewards freigeschaltet sein und anschließend nacheinander geclaimed werden.

Der Spieler soll den Reward trotzdem nur einmal pro Day bekommen.

Das System muss serverseitig prüfen, ob der Reward bereits geclaimed wurde.

Ein Spieler darf nicht durch RemoteEvent-Spam denselben Reward mehrfach bekommen.

Die Daily-Reward-Daten müssen gespeichert und beim nächsten Join korrekt geladen werden.

Speichere mindestens die für die Fortschrittsberechnung notwendigen Zeit- und Claim-Daten.

Die Logik muss auch nach Server-Neustarts funktionieren.

Die vergangene Zeit soll der entscheidende Faktor sein und nicht die Anzahl der tatsächlichen Joins.


DAILY-REWARD-UI-ANIMATION

Der aktuell claimbare und noch nicht geclaimte Day soll eine langsame Tween-Animation haben.

Die Animation soll langsam größer werden und danach langsam wieder kleiner werden.

Danach soll sie wieder von vorne beginnen.

Die Animation soll kontinuierlich und ruhig in einem Loop laufen.

Wenn ein Day bereits geclaimed wurde:

claimedlabel.Visible = true

ImageTransparency des Buttons = 0.5

Es darf keine Tween-Animation mehr laufen.

Wenn ein Day noch nicht geclaimed wurde und aktuell claimbar ist:

claimedlabel.Visible = false

normale ImageTransparency

langsame Tween-Animation aktiv

Nur tatsächlich claimbare und nicht geclaimte Days sollen animiert werden.

Es dürfen keine mehreren parallelen Tween-Loops für denselben Day entstehen.

Wenn ein Reward geclaimed wurde, muss eine eventuell laufende Tween-Animation sauber beendet werden.


WING-SYSTEM UND MULTIPLIKATOREN

Das Wing-System soll bei den Multiplikatoren vollständig geändert werden.

Ein Wing mit 2x Multi soll nicht mehr Ascend Speed verwenden.

Die alte Ascend-Speed-Logik darf für den Wing-Multi nicht mehr verwendet werden.

Der Wing-Multi soll direkt den XP- und Fortschrittszuwachs beeinflussen.

Beim normalen Laufen gilt:

ohne Multi = +1
Wing mit 2x Multi = +2
Wing mit 3x Multi = +3

Das gilt für die entsprechenden Werte wie JumpXP und SpeedXP.

Treadmills haben ebenfalls einen eigenen Multiplikator.

Der Treadmill-Multi und der Wing-Multi müssen addiert werden.

Sie dürfen nicht miteinander multipliziert werden.

Beispiel:

Treadmill-Multi = 2
Wing-Multi = 3

Ergebnis = 5

Dann soll die Auszahlung immer +5, +5, +5 usw. sein.

Nicht 6.

Allgemein:

Gesamt-Multi = Treadmill-Multi + Wing-Multi

Weitere Beispiele:

1 + 2 = 3
2 + 2 = 4
2 + 3 = 5
5 + 3 = 8

Die gleiche Logik muss auch bei Trampolinen verwendet werden.

Der Wing-Multiplikator darf dort ebenfalls nicht mehr über Ascend Speed umgesetzt werden.

Die tatsächliche XP-Auszahlung muss mit dem berechneten Gesamt-Multiplikator arbeiten.

Suche alle relevanten Stellen im bestehenden Wing-, Treadmill-, Trampoline-, JumpXP- und SpeedXP-System und entferne die alte Ascend-Speed-basierte Multiplikatorlogik.


EVENT-SYSTEM SMOOTHER MACHEN UND LAGS BEHEBEN

Das Event-System ist aktuell buggy und verursacht viele Lags.

Problem 1:

Die Farben ändern sich aktuell abgehackt.

Der Farbwechsel soll smooth erfolgen.

Nutze dafür eine performante Tween- oder Interpolationslösung, sodass zwischen den Farben keine sichtbaren Sprünge auftreten.

Problem 2:

Das Event-System verursacht zu viele Lags.

Behebe die Performance-Probleme.

Nutze dafür das neue Chunk-System, das bereits in meinem ChunkLoader vorhanden ist.

Erstelle kein zweites unnötiges Chunk-System.

Vermeide insbesondere:

unnötige while-loops
unnötige Heartbeat-Verbindungen
unnötige RenderStepped-Verbindungen
unnötige RemoteEvent-Aufrufe
unnötiges Scannen großer Teile des Workspace
doppelte Event-Handler
wiederholtes Setzen identischer Properties
unnötige Tween-Erzeugungen

Das Event soll für den Spieler smooth aussehen und gleichzeitig deutlich weniger Performance kosten.


STAMINA-RELOAD-SYSTEM

Das Reload-System der Stamina-Bar soll immer ein konstantes Verhältnis zwischen MaxStamina und ReloadRate verwenden.

Beispiel:

MaxStamina = 50
ReloadRate = 25

Dann gilt dieses Verhältnis auch bei höheren MaxStamina-Werten.

Wenn MaxStamina später 100 beträgt, soll die ReloadRate 50 betragen.

Die Reload-Geschwindigkeit soll also proportional mit der maximalen Stamina skalieren.

Allgemein soll gelten:

effektive ReloadRate = Basis-ReloadRate × aktuelles MaxStamina / Referenz-MaxStamina

Dadurch darf hohe MaxStamina nicht dazu führen, dass das Aufladen extrem lange dauert.

Das Verhältnis soll immer gleich bleiben.

Der normale Stamina-Verbrauch darf dadurch nicht unbeabsichtigt verändert werden.


WINS AUF WIN-PLATTFORMEN MÜSSEN GANZZAHLIG SEIN

Win-Werte auf den Win-Plattformen dürfen niemals Dezimalzahlen enthalten.

Beispiele:

1.1 soll zu 2 werden
1.5 soll zu 2 werden
2.01 soll zu 3 werden

Es soll immer auf die nächste ganze Zahl aufgerundet werden.

Das muss für:

Berechnung
Vergabe
Speicherung
Anzeige

gelten.

Es darf niemals etwas wie 1.5 oder 2.75 gespeichert oder angezeigt werden.


GETBACK-POPUP UND FREEGIFT RESPONSIVE TEXTGRÖSSE

Die TextLabels im GetBack-Popup und im FreeGift-Popup sind auf kleineren Geräten viel zu groß.

Diese TextLabels sollen sich exakt nach demselben Prinzip responsiv skalieren wie die TextLabels in den Bars.

Insbesondere das Label, bei dem beispielsweise WingStamina angezeigt wird, funktioniert bereits perfekt.

Verwende dessen bestehendes Responsive-/UIScale-Verhalten als Referenz.

Die GetBack- und FreeGift-TextLabels sollen auf:

PC
Laptop
Mobile
Tablet
Console
VR
anderen Auflösungen

korrekt skaliert werden.

Nicht einfach nur für Mobile eine kleinere feste TextSize setzen.

Das bestehende responsive System soll verwendet werden.

Fonts und Designs dürfen nicht verändert werden.


RANK-SYSTEM

In ReplicatedStorage gibt es einen Ordner namens:

ranks

Darin befinden sich Modelle.

Die Namen dieser Modelle sind die Rangnamen.

Beispiele:

creator
helper
owner
admin

und weitere.

Die exakten Modellnamen sollen als Rank-Namen verwendet werden.


NEUER ADMIN-COMMAND FÜR RÄNGE

Es soll einen neuen Command geben:

/rang set [rangname] [username/target] [state]

Beispiele:

/rang set creator PlayerName true

/rang set helper PlayerName false

/rang set admin me true

/rang set helper all false

/rang set creator others true

Der Rangname muss exakt einem Modellnamen aus ReplicatedStorage.ranks entsprechen.

Der Target-Teil soll das vorhandene Target-System verwenden.

Folgende Targets müssen funktionieren:

me
all
others
konkreter Spielername

Bei einem normalen Username muss zusätzlich eine globale Roblox-Username-Suche möglich sein.

Der Username darf also auch gefunden werden, wenn die Person gerade nicht auf demselben Server ist.

Die serverseitige Lösung muss den gefundenen UserId sauber verwenden.

State:

true = Rang geben
false = Rang entfernen


RANG-MODELL ÜBER DEM SPIELER

Wenn ein Spieler einen Rang besitzt, soll das entsprechende Modell aus:

ReplicatedStorage.ranks

über dem Kopf des Spielers angezeigt werden.

Beispiel:

Rang creator

Dann soll das Modell:

ReplicatedStorage.ranks.creator

über seinem Kopf erscheinen.

Der Y-Offset soll ungefähr 3 Studs über dem Kopf liegen.

Das Modell muss korrekt mit dem Character verbunden bzw. positioniert werden.

Beim Respawn muss der Rang wieder automatisch erscheinen.

Beim Ändern des Rangs muss das alte Rangmodell entfernt werden.

Beim Entfernen des Rangs muss das Modell verschwinden.

Es dürfen niemals mehrere Rank-Modelle gleichzeitig für denselben Spieler erzeugt werden.


NEUER ADMIN-COMMAND /edit ranks

Es soll einen neuen Command geben:

/edit ranks [rank]

Beispiele:

/edit ranks helper

/edit ranks admin

/edit ranks creator

Beim Ausführen soll automatisch ein Rank-Berechtigungsmenü erstellt bzw. geöffnet werden.

Das Menü soll nicht manuell im Studio gebaut werden müssen, sondern vom Script erstellt werden, sofern es noch nicht existiert.


DESIGN DES RANK-BERECHTIGUNGSMENÜS

In der Mitte soll ein rechteckiger ScrollingFrame erscheinen.

Der ScrollingFrame soll:

schwarz sein
ungefähr 0.5 Transparency haben
mittig platziert sein
responsiv sein

Innerhalb des ScrollingFrames sollen die einzelnen Berechtigungen angezeigt werden.

Beispielsweise:

send_announcements       true
start_events_local       false
stop_events_local        false
start_events_global      false
stop_events_global       false
give/set_speedxp         true
give/set_staminalevel    true
give/set_jumpxp          true

und alle weiteren relevanten Admin-Berechtigungen.

Der vorhandene Font und das bestehende Font-Design sollen nicht verändert werden.

Die Textfarbe soll weiß sein.

Neben bzw. über dem true/false-Wert soll ein unsichtbarer Button liegen.

Beim Anklicken soll sich der Zustand ändern:

true zu false

oder

false zu true

Die Anzeige soll sofort aktualisiert werden.


STANDARD-BERECHTIGUNGEN

Für owner sollen standardmäßig alle Berechtigungen auf true stehen.

Für alle anderen Ränge sollen standardmäßig alle Berechtigungen auf false stehen.

Die Berechtigungen müssen für jeden Rank separat gespeichert werden.

Beispiel:

helper.send_announcements = false
helper.give_speedxp = true

Ein Rank darf also seine eigenen Berechtigungseinstellungen haben.


RANK-PERMISSIONS MÜSSEN DAS ADMIN PANEL WIRKLICH STEUERN

Die Rank-Permissions dürfen nicht nur optisch sein.

Sie müssen tatsächlich die verwendbaren Admin-Funktionen kontrollieren.

Beispiel:

Wenn helper.send_announcements = false ist, dann darf ein Helper:

/announce nicht ausführen
/announce im Admin Panel nicht sehen
keine Suggestion für /announce bekommen

Wenn helper.give_speedxp = true ist, dann darf ein Helper die entsprechende Funktion sehen und benutzen.

Das gleiche Prinzip muss für alle vorhandenen Commands und Admin-Funktionen gelten.

Der Client darf nur erlaubte Commands anzeigen.

Zusätzlich muss der Server bei jedem Admin-Command die Berechtigung erneut prüfen.

Ein Spieler darf nicht durch manuelles Senden eines RemoteEvents eine verbotene Admin-Funktion ausführen können.

Die Berechtigungsprüfung muss serverseitig autoritativ sein.


RANK-BERECHTIGUNGEN MIT Z SPEICHERN

Wenn das /edit ranks Menü geöffnet wurde, soll die Taste Z die aktuellen Einstellungen speichern.

Beim Drücken von Z:

aktuelle Berechtigungen speichern
Daten persistent speichern
Menü schließen
keine anderen UI-Systeme schließen

Die Berechtigungen müssen nach einem Server-Neustart weiterhin vorhanden sein.

Beim nächsten Laden müssen die gespeicherten Einstellungen des jeweiligen Rangs wieder geladen werden.


WING-KAUF MUSS REBIRTH-ANZEIGE AKTUALISIEREN

Wenn ein Spieler einen Wing kauft, müssen die benötigten Rebirths korrekt abgezogen werden.

Danach muss die Rebirth-Anzeige im UI sofort aktualisiert werden.

Aktuell wird der Rebirth-Wert beim Kauf nicht korrekt angezeigt bzw. nicht korrekt abgezogen.

Der Ablauf muss daher korrekt sein:

Kaufanfrage
Server prüft Preis
Server prüft ausreichende Rebirths
Server zieht Rebirths ab
Daten werden aktualisiert
UI wird aktualisiert

Die Serverdaten sind die Wahrheit.

Der Client darf nicht einfach selbst einen Wert setzen, ohne dass der Server den Kauf bestätigt.

Nach dem Kauf müssen auch alle anderen Systeme weiterhin den aktuellen Rebirth-Wert verwenden.


SETTINGS-POPUP SLIDER

Im Settings-Popup gibt es einen Slider und ein Prozent-Label.

Aktuell wird nur der Prozentwert gespeichert, aber nicht korrekt die X-Position des Sliders geladen.

Beispiel:

Gespeichert:

50 Prozent

Nach dem Laden:

Label zeigt 50 Prozent

Slider steht aber bei 100 Prozent

Das darf nicht passieren.

Der gespeicherte Prozentwert soll die zentrale Datenquelle sein.

Beim Laden muss daraus exakt die richtige X-Position berechnet werden.

Beispiele:

0 Prozent = X.Scale 0
25 Prozent = X.Scale 0.25
50 Prozent = X.Scale 0.5
75 Prozent = X.Scale 0.75
100 Prozent = X.Scale 1

Das Verhältnis zwischen gespeicherten Prozentwert und Sliderposition muss immer exakt stimmen.


SLIDER MOBILE-PROBLEM

Auf Handy wird beim Bewegen des Sliders die Y-Achse plötzlich viel zu groß.

Das darf nicht passieren.

Beim Draggen des Sliders darf ausschließlich die horizontale X-Position verändert werden.

Die Y-Position muss unverändert bleiben.

Das bedeutet:

X darf verändert werden.

Y.Scale darf nicht verändert werden.

Y.Offset darf nicht verändert werden.

Das UIScale-System muss weiterhin vollständig auf das UI wirken.

Die Slider-Bewegung darf das bestehende Responsive-System nicht überschreiben.

Der Slider muss auf PC und Mobile dieselbe logische Prozentposition darstellen.

Beispiel:

50 Prozent auf PC = Slider Mitte

50 Prozent auf Mobile = Slider ebenfalls exakt Mitte

Die Y-Größe und Y-Position müssen dabei weiterhin korrekt responsive bleiben.


DATASTORE UND BESTEHENDE DATEN

Alle neuen Daten sollen sauber in die bestehende DataStore-Struktur integriert werden.

Bestehende Spielerdaten dürfen nicht gelöscht werden.

Alte Daten müssen weiterhin funktionieren.

Wenn die DataStore-Struktur erweitert wird, sollen fehlende neue Werte automatisch mit sinnvollen Standardwerten initialisiert werden.

Daily-Rewards, Rank-Permissions und andere neue persistent gespeicherte Werte dürfen keine bestehenden Daten überschreiben.

Die Speicherung muss robust gegen Fehler sein.


PERFORMANCE UND SAUBERE ARCHITEKTUR

Achte bei allen Änderungen darauf, dass keine doppelten Systeme entstehen.

Besonders wichtig:

keine doppelten Loops
keine doppelten RemoteEvents
keine doppelten Character-Connections
keine unnötigen RenderStepped-Loops
keine unnötigen Heartbeat-Loops
keine unnötigen Tweens
keine redundanten UI-Updates
keine unnötigen Workspace-Scans

Verwende vorhandene Caches, Chunk-Systeme, Remotes und Module, sofern sie bereits existieren.


TESTFÄLLE DAILY REWARDS

Neuer Spieler kann Day 1 claimen.

Day 1 gibt exakt 5 Speed.

Day 1 kann nicht zweimal geclaimed werden.

Am nächsten Tag wird Day 2 freigeschaltet.

Day 2 gibt exakt 10 Wins.

Wenn der Spieler mehrere Tage nicht online war, geht der Fortschritt trotzdem weiter.

Verpasste Tage gehen nicht verloren.

Mehrere freigeschaltete Days bleiben verfügbar.

Geclaimte Days zeigen das ClaimedLabel.

Geclaimte Days haben ImageTransparency 0.5.

Geclaimte Days haben keine Animation mehr.

Aktuell claimbare und nicht geclaimte Days haben die langsame Tween-Animation.


TESTFÄLLE WING MULTI

Normales Laufen funktioniert mit Wing Multi.

JumpXP funktioniert mit Wing Multi.

SpeedXP funktioniert mit Wing Multi.

Treadmill Multi und Wing Multi werden addiert.

Trampoline Multi und Wing Multi werden addiert.

Es darf an keiner Stelle mehr die alte Ascend-Speed-Multiplikatorlogik verwendet werden.


TESTFÄLLE EVENT

Farbwechsel sind smooth.

Keine sichtbaren Farbsprünge.

Event verursacht deutlich weniger Lag.

Chunk-System wird verwendet.

Keine unnötigen dauerhaften Loops.


TESTFÄLLE STAMINA

Reload-Verhältnis bleibt konstant.

Mehr MaxStamina führt proportional zu mehr ReloadRate.

50 MaxStamina mit 25 ReloadRate bedeutet bei 100 MaxStamina entsprechend 50 ReloadRate.

Kein extrem langsames Aufladen bei hoher MaxStamina.


TESTFÄLLE WINS

Keine Dezimalwerte.

1.1 wird 2.

1.5 wird 2.

2.01 wird 3.

Die Werte werden auch bei Speicherung und Anzeige ganzzahlig behandelt.


TESTFÄLLE RANKS

Rang geben funktioniert.

Rang entfernen funktioniert.

me funktioniert.

all funktioniert.

others funktioniert.

Konkreter Username funktioniert.

Globale Username-Suche funktioniert.

Rank-Modell erscheint über dem Kopf.

Rank-Modell verschwindet beim Entfernen.

Rank-Modell erscheint nach Respawn erneut.

Keine doppelten Rank-Modelle.

Owner hat standardmäßig alle Permissions true.

Andere Ränge haben standardmäßig alle Permissions false.

Berechtigungen können mit dem Menü verändert werden.

Z speichert die Änderungen.

Gespeicherte Permissions bleiben nach Neustart erhalten.

Nicht erlaubte Commands werden im Admin Panel nicht angezeigt.

Nicht erlaubte Commands können serverseitig nicht ausgeführt werden.


TESTFÄLLE WING-KAUF

Wing-Kauf funktioniert.

Rebirth-Preis wird korrekt abgezogen.

Rebirth-Anzeige aktualisiert sich sofort.

Gespeicherte Rebirth-Daten stimmen nach erneutem Join.


TESTFÄLLE SLIDER

0 Prozent = ganz links.

25 Prozent = 25 Prozent.

50 Prozent = Mitte.

75 Prozent = 75 Prozent.

100 Prozent = ganz rechts.

Gespeicherte Werte werden beim Laden korrekt auf die Sliderposition übertragen.

Mobile verändert beim Draggen nicht die Y-Achse.

PC und Mobile behalten korrekte Proportionen.


ABSOLUT WICHTIG

Analysiere zuerst meine vorhandenen Scripts und finde heraus, welche Scripts tatsächlich für die jeweiligen Systeme verantwortlich sind.

Verändere nicht blind irgendwelche Scripts.

Ordne jede Änderung dem richtigen System zu.

Wenn mehrere Scripts zusammenarbeiten, müssen sie gemeinsam korrekt angepasst werden.

Keine halb fertigen Patches.

Keine ausgelassenen Teile.

Keine Aussagen wie "füge deinen vorhandenen Code hier ein".

Keine vereinfachten Beispielcodes.

Liefere am Ende die vollständigen benötigten Scripts, die ich direkt in Roblox Studio ersetzen oder einfügen kann.

Alle bestehenden funktionierenden Systeme müssen erhalten bleiben, sofern sie nicht ausdrücklich durch eine der oben beschriebenen Änderungen ersetzt werden.

Die neuen Systeme müssen mit der bestehenden Game-Architektur kompatibel sein.

Achte besonders darauf, keine alten Versionen der Systeme parallel laufen zu lassen.
